#include "main.h"

/**
 * _mul - multiplies two integers
 * @a: first digit to be multiplied
 * @b: second digit to be multiplied
 *
 * Return: Always the product of the integers
 */
int _mul(int a, int b)
{
	return (a * b);
}
