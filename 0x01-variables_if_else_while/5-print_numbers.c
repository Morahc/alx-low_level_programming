#include <stdio.h>

/*
* main - prints all single digit base 10 number
* Returns: 0 on succes
*/

int main(void)
{
	for(int i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	putchar('\n');

	return (0);
}
